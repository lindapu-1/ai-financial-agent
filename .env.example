# -----------------------------------------------------------------------------
# Hony Capital AI Agent - 本地开发环境配置
# -----------------------------------------------------------------------------

# 1. 基础配置 (必须)
# 只要保证不为空即可，或者运行 openssl rand -base64 32 生成一个
AUTH_SECRET=hony_capital_secret_key_123

# 2. 数据库配置 (默认指向 Docker 本地数据库)
# 如果你按照 README 中的命令启动 Docker，这里不需要改动
# 注意端口是 5433
POSTGRES_URL=postgres://postgres:postgres@localhost:5433/hony_agent

# 3. AI 模型配置 (必须)
# 在这里填入你的 API Key
OPENAI_API_KEY=
GOOGLE_API_KEY=
DEEPSEEK_API_KEY=

# 4. 金融数据插件 (可选)
# 用于获取实时股票财报数据: https://financialdatasets.ai/
FINANCIAL_DATASETS_API_KEY=
